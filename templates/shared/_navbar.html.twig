<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('app_index')}}">LSMS</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor02">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					{% if not is_granted('IS_AUTHENTICATED_FULLY') %}
						<a href="{{path('app_login')}}" class="nav-link">Connexion</a>
					{% else %}
						<li class="nav-item">
							<a href="{{path('app_civils_tarif')}}" class="nav-link">Tarif</a>
						</li>
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
								Rendez vous
							</a>
							<ul class="dropdown-menu">
								<li>
									<a class="dropdown-item" href="{{path('app_civils_appointment_add_appointment')}}">Prise de rendez vous</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{path('app_civils_planning')}}">Planning</a>
								</li>
							</ul>
						</li>
						{% if  is_granted('ROLE_STAFF') %}
							<li class="nav-item">
								<a href="{{path('app_staff')}}" class="nav-link">Admin</a>
							</li>
						{% endif %}
						<a href="{{path('app_logout')}}" class="nav-link">Déconnexion</a>
					{% endif %}
				</li>
			</ul>
		</div>
	</div>
</nav>
